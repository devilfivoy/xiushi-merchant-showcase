# 店播标杆案例 Onepage 生成器

## 概述

将小红书电商店播案例复盘文档（Redoc / PDF / 文本）转化为**对外可宣传的脱敏 onepage 长图 HTML**，PPT slide 风格，支持桌面/平板/手机三档自适应，右上角可下载高清 PNG。

适用场景：行业运营、商家运营向外部商家展示标杆案例时使用。

## 输入

- 一份或多份案例复盘文档（Redoc 链接 / PDF / 纯文本）
- 每份文档包含：商家名称、直播类型（中场/大场）、核心数据、关键动作、改进建议、行业可复制洞察

## 输出

每个案例生成一页独立的 onepage HTML，结构固定如下：

```
┌──────────────────────────────────────┐
│ 小红书电商 · 店播标杆案例    2026年X月  │  ← 顶部标识栏
├──────────────────────────────────────┤
│ ▎主标题（红色竖条 + 大号加粗）          │  ← 主标题区
├──────────────────────────────────────┤
│ [商家胶囊标签]  商家描述文案            │  ← 商家信息卡
├──────────────────────────────────────┤
│  40万+  │  >30%  │  ~60%  │  >80%   │  ← KPI 数据条（红底线+奇偶色差）
├──────────────────────────────────────┤
│  亮点优势                             │  ← 红底白字胶囊 + 编号分点
│  ① xxxxxxxxx  ② xxxxxxxxx  ③ xxx    │
├──────────────────────────────────────┤
│  三个关键动作                          │  ← 三列动作卡（渐变进度条）
│  [卡1] [卡2] [卡3]                    │
├──────────────────────────────────────┤
│  ⚠️ 优化建议（如有）                   │  ← 橙色边框提示条
├──────────────────────────────────────┤
│  💡 行业可复制                         │  ← 三列粉底圆角卡
│  ① xxx  ② xxx  ③ xxx                │
├──────────────────────────────────────┤
│  数据来源 · 小红书电商          小红书电商 │  ← 底部
└──────────────────────────────────────┘
```

## 工作流程

### 第一步：抓取原文档
- 用 `web_fetch` 抓取 Redoc 文档内容（或读取 PDF/文本文件）
- 完整提取所有数据点、分析结论、改进建议

### 第二步：脱敏确认
必须和用户确认以下脱敏口径，不能自行决定：

| 脱敏维度 | 常见选项 |
|---------|---------|
| GMV 金额 | A.约数 B.量级区间 C.倍数表达 |
| 转化率/占比 | A.取整 B.约数 C.分档表达 |
| 商家名称 | 真名 / 部分脱敏（如"札x旗舰店"）/ 完全脱敏 |
| 绝对值（人数等）| 保留 / 约数 / 只保留占比 |

**脱敏后制作脱敏对照表（仅供内部核对，不出现在成品中）。**

### 第三步：文案定稿
- 基于原文档表达撰写文案，尽量保留原文的措辞和逻辑
- 表达面向商家，通俗易懂
- 遵守写作规则：
  - 禁止「不是……而是……」句式，直接正面表述
  - 禁止使用冒号（：）作为句中停顿
  - 禁止使用破折号（——）
- **完整覆盖原文档所有板块**：核心数据 → 亮点优势 → 关键动作（原文有几个写几个）→ 改进建议/优化建议（如有）→ 行业可复制
- 文案整理完毕后发给用户确认，**确认后再做图**

### 第四步：生成 HTML
- 设计风格参考 PPT slide 风格（白底、红色 `#E60033` 主色、粉色 `#FCE4EC` 辅色）
- 布局以内容驱动，不死板套模板
- 三档自适应：桌面（max-width 1280px）、平板（≤960px）、手机（≤520px）
- 关键 CSS 组件：
  - 顶部标识栏（左 logo 文字、右日期，底部分割线）
  - 红色竖条标题
  - 圆角胶囊标签（粉底红字）
  - KPI 数据条（底部红色渐变线、奇偶行色差）
  - 亮点优势编号分点（红底白字圆形序号）
  - 动作卡三列并排（顶部色条 + 进度条数据可视化）
  - 优化建议橙色提示条
  - 行业可复制三列粉底卡

### 第五步：预览 & 上传
- 本地 HTTP server 预览确认
- 上传到腾讯云 COS CDN（`aibibp-cdn-upload` skill）
- 上传到 GitHub 仓库并开启 GitHub Pages

## 设计规范

### 配色
| 用途 | 色值 |
|------|------|
| 主色（标题/数据/标签） | `#E60033` |
| 辅色（标签底色/卡片底色） | `#FCE4EC` / `#FFF5F5` |
| 动作卡二号色 | `#FF6B6B` |
| 动作卡三号色 | `#D84315`（或 `#2E7D32` 用于蓄水/自然流量语境） |
| 优化建议色 | `#FF8F00`（橙色） |
| 正文色 | `#1a1a1a` / `#333` / `#555` |
| 辅助色 | `#999` / `#ccc` |
| 背景 | `#fff`（卡片）/ `#e8e6e3`（页面底） |

### 字体
```css
font-family: -apple-system, "PingFang SC", "Helvetica Neue", "Microsoft YaHei", sans-serif;
```

### 尺寸
- 桌面 max-width: 1280px，padding: 52px 64px
- 平板 padding: 32px 28px
- 手机 padding: 24px 16px

## 关联 Skill
- `data-desensitize`：数据脱敏参考
- `aibibp-cdn-upload`：上传到腾讯云 COS CDN
- `xhs-case-onepage-heqianni`：贴纸风 onepage 参考（本 skill 在此基础上演化为 PPT slide 风格）
- `external-merchant-poster`：对外商家海报参考

## 示例成品

### 案例一：札x旗舰店 · 7月中场
- 主题：用新品做预约钩子，单品聚焦打出 10 倍日播的中场爆发力
- CDN: `https://picasso-private-1251524319.cos.ap-shanghai.myqcloud.com/formula-static/dibp/w54izo2v/onepage-case1.html`

### 案例二：内蒙xx的店 · 8月大场
- 主题：「猪爆脆」驱动的店播「上新日」打法，单场 GMV 40万+，日播的 20 倍
- CDN: `https://picasso-private-1251524319.cos.ap-shanghai.myqcloud.com/formula-static/dibp/o3fysvkn/onepage-case2.html`

## 注意事项
1. **所有数据必须来自原文档**，不能编造或推断
2. **脱敏口径必须和用户确认**，不能自行决定
3. **文案确认后再做图**，不要跳步
4. **文案内容完整覆盖原文档**，不能遗漏关键动作或改进建议
5. **不同案例的数据/结论不能混用**
